global using System;
global using NUnit.Framework;

namespace Test
{
    /*
     * Verwendet wird nUnit.
     * Tutorial: https://docs.microsoft.com/de-de/dotnet/core/testing/unit-testing-with-nunit
     * 
     * Drei Grundsätze des Testens:
     *  - Man findet nur die Fehler nach denen man auch sucht.
     *  - Ein Testfall muss möglichst einfach sein.
     *  - Ein Testfall sollte möglichst unabhängig und eindeutig sein. 
     *  
     *  (Auch Testfälle können falsch sein. Da sie nicht getestet werden können/sollten
     *  müssen Testfälle möglichst leicht auf ihre Richtigkeit überprüfbar sein.)
     */
}